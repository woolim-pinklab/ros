# 게임 참가/퇴장 서비스 — 이름을 적어 참가하면 거북이가 생기고
# 머리 위에 이름표가 달린다. 서비스(요청→응답) 학습용.
#
# 토큰은 없다. 같은 도메인(또는 브라우저 뷰어)에 있으면 이름만으로 조종한다.
string name      # 내 이름 (참가 시 필수, 중복 불가 / 퇴장 시에도 이름으로 찾는다)
---
bool ok          # 성공 여부
string message   # 실패 이유 또는 안내 문구
